# 项目总结

## 1. 项目背景

本项目基于淘宝公开用户行为数据集，对用户浏览、收藏、加购、购买等行为进行系统分析，目标是识别用户转化路径中的关键流失环节，并通过用户分层分析支持电商运营优化与转化提升。

## 2. 核心结论

1. 浏览行为远高于购买行为，说明整体转化链路较长，存在明显损耗。
2. 用户活跃高峰集中在晚间时段，适合安排消息触达、营销投放和直播活动。
3. 加购到购买环节流失明显，是最值得优先优化的转化环节之一。
4. 高价值用户占比较低，应强化会员体系、复购激励和重点用户运营。

## 3. 数据说明

原始数据文件为 `UserBehavior.csv`，主要字段包括：

* `user_id`：用户 ID
* `item_id`：商品 ID
* `category_id`：商品品类 ID
* `behavior_type`：行为类型，包含 `pv`、`fav`、`cart`、`buy`
* `timestamp`：行为发生时间戳

由于原始数据体量较大，项目中设计了抽样逻辑：当数据量超过 500 万行时，随机抽样 500 万行用于本地分析和作品集复现，`random_state=42` 用于保证结果可复现。

## 4. 分析流程

1. 读取原始 CSV 并补充字段名。
2. 检查数据规模、字段类型、缺失值和重复值。
3. 将时间戳转换为标准时间，并扩展日期、小时、星期字段。
4. 输出清洗结果供 EDA、SQL 和 Power BI 复用。
5. 完成行为分布、活跃时段、转化漏斗和用户分层分析。
6. 导出看板所需数据文件，支持 Power BI 展示。

## 5. 指标体系与 AARRR 映射

虽然本项目主要使用行为明细数据，但分析逻辑可以映射到 AARRR 框架：

* `Acquisition`：通过浏览行为 `pv` 观察用户进入商品页后的初始规模。
* `Activation`：通过收藏 `fav` 和加购 `cart` 衡量用户兴趣与购买意愿。
* `Revenue`：通过购买行为 `buy` 衡量最终转化结果。
* `Retention`：通过用户分层与复购分析观察持续购买表现。
* `Referral`：原始数据缺少分享与推荐字段，因此未做推荐传播分析。

核心指标包括：

* 总用户数
* 总行为数
* 总购买数
* 浏览到购买转化率
* 收藏到购买转化率
* 加购到购买转化率
* 用户活跃高峰时段
* 高价值用户占比
* 复购用户分布

## 6. 方法说明

### 为什么抽样

抽样是为了本地复现效率与作品集可运行性。相比直接处理完整原始数据，500 万行样本更适合个人电脑、Notebook 演示和 GitHub 项目展示。

### 为什么使用简化版 RF 模型

原始数据缺少成交金额字段，因此无法构建完整 RFM 模型中的 `Monetary` 维度。本项目采用简化版 `RF` 模型：

* `Recency`：距离最近一次购买的天数
* `Frequency`：购买次数

这已经足以支持高价值用户识别与基础用户运营分层。

### 为什么做漏斗分析

漏斗分析主要用于识别关键流失环节。相比单纯统计购买数量，漏斗更能反映用户从兴趣到成交的过程损耗。

### 为什么做活跃时段分析

活跃时段分析用于支持投放与触达策略，比如优惠券发放、直播活动排期、Push 发送时机和购物车召回时间选择。

## 7. 转化漏斗分析

项目围绕浏览、收藏、加购、购买四个关键阶段构建转化漏斗，用于回答以下问题：

* 浏览量是否远高于后续行为量
* 收藏与加购是否对购买形成有效促进
* 哪一阶段流失最明显
* 运营优化优先级应该放在哪个环节

这部分结果可直接支持详情页优化、下单链路优化、加购召回和优惠策略设计。

## 8. 用户分层分析

本项目基于购买行为构建简化版 RF 用户分层模型，其中：

* `Recency`：最近购买时间越近越好
* `Frequency`：购买次数越多越好

结合分位数打分，将用户划分为：

* 高价值用户
* 潜力用户
* 沉睡用户

该结果可用于会员运营、精准营销、重点用户维护和流失召回。

## 9. 复购与 Cohort 增强建议

### 复购分析

当前项目已经具备购买行为明细，后续建议补充：

* 购买 1 次用户占比
* 购买 2 次及以上用户占比
* 高频购买用户分布
* 首购用户向复购用户的转化比例

这类分析可以让项目从“首购转化分析”升级到“用户生命周期分析”。

### Cohort 分析

建议以后续版本增加 cohort 留存分析：

* 以用户首次购买日期作为 cohort 划分标准
* 观察后续周/月的再次购买表现
* 对比不同批次用户的留存差异

这一步能显著提升项目在中高阶数据分析岗位中的说服力。

## 10. Power BI 看板说明

Power BI 看板建议拆分为以下页面：

1. 运营总览
2. 用户转化漏斗
3. 用户活跃时间分析
4. 用户画像分析
5. 业务结论

数据源来自清洗与分析阶段导出的 CSV 文件，便于快速搭建作品集展示页面。

## 11. 业务优化建议

1. 针对浏览量高但购买率低的商品，优化详情页内容和促销信息。
2. 在晚间高活跃时段集中投放优惠券、直播活动或消息提醒。
3. 针对加购未购买用户设计限时折扣和购物车召回策略。
4. 对高价值用户设置会员权益和专属优惠，提升复购率。
5. 对沉睡用户制定召回活动，降低用户流失。

## 12. 分析局限性

1. 原始数据不包含成交金额，因此无法构建完整 RFM 模型中的 M 维度。
2. 数据来自公开样本，不能完全代表真实电商平台生产环境。
3. 抽样策略更适合趋势分析和作品集展示，不适合精确结算级分析。
4. 缺少渠道、价格、活动、用户属性等字段，暂时无法做更细的归因分析和精细化分群。

## 13. 简历包装话术

项目名称：电商用户行为分析与转化漏斗优化项目  
技术栈：Python / Pandas / SQL / Power BI / Excel

可用于简历的项目描述示例：

* 基于淘宝公开用户行为数据集，完成 500 万+ 行为记录的数据清洗、抽样与探索性分析。
* 搭建浏览-收藏-加购-购买转化漏斗，识别关键流失环节并输出转化优化方向。
* 基于购买行为构建简化版 RF 用户分层模型，完成高价值用户、潜力用户和沉睡用户识别。
* 使用 SQL 和 Power BI 搭建可视化看板，实现核心指标、用户活跃时段和用户画像展示。
* 结合分析结果提出精准营销、购物车召回和高价值用户运营建议。
